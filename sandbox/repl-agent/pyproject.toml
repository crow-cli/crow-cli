[project]
name = "repl-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "crow-cli",
    "fastmcp",
    "pytest",
    "pytest-asyncio",
]

[tool.uv.sources]
crow-cli = { path = "../../crow-cli" }

[tool.pytest.ini_options]
asyncio_mode = "auto"
