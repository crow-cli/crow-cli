[project]
name = "repl-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "crow-cli",
]

[tool.uv.sources]
crow-cli = { path = "../../crow-cli" }
