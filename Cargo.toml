[workspace]
resolver = "2"
members = [
    "crow-memory",
    "crow-memory-types",
]

# The Python packages (crow-cli, crow-mcp, crow-memory-sdk, ...) are the
# clients of this workspace. Only the crow-memory HTTP service and its wire
# types live in Rust; the SDK is python (crow-memory-sdk/).
