[project]
name = "crow-memory-mcp"
version = "0.1.0"
description = "Crow memory MCP server for querying agent conversation history"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.3.1",
    "sqlalchemy>=2.0.49",
]


